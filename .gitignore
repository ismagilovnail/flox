# --- Secrets / env ---
.env
.env.*
!.env.example
*.pem
*.key
secrets/

# --- Node / Next.js (apps/web) ---
node_modules/
.next/
out/
dist/
build/
.turbo/
*.tsbuildinfo
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.pnpm-store/
coverage/
.eslintcache

# --- Go (apps/api, tracker, worker) ---
bin/
*.exe
*.test
*.out
vendor/
# compiled binaries by module name
apps/api/api
apps/tracker/tracker
apps/worker/worker

# --- Data stores / local infra ---
*.db
*.sqlite
*.sqlite3
pgdata/
clickhouse-data/
redis-data/
minio-data/
docker-volumes/

# --- Object storage / uploads (local) ---
uploads/
tmp/
*.log

# --- Local reference material (not part of the project) ---
# Read-only source code from another tracker, kept locally for comparison.
# ~600MB including node_modules, .next and compiled binaries — never commit it.
# Findings from it live in docs/spec-amendments-phase22.md instead.
reference/

# --- Editors / OS ---
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# --- Test / build artifacts ---
playwright-report/
test-results/
.cache/
