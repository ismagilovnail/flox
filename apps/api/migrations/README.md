# migrations

PostgreSQL schema migrations, managed with [goose](https://github.com/pressly/goose).
Empty until Phase 17 (Database) creates the first migration — `organizations`,
`users`, `memberships`, and the rest of §35's core table list, using ULID
primary keys throughout (§35, §36-TENANCY).
