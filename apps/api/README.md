# apps/api

Go control-plane API (chi, pgx). Scaffolded starting Phase 16 (Go Backend
Foundation). Shares `internal/routing` and `internal/classifier` with
`apps/tracker` and `apps/worker`. Placeholder directory until then.
